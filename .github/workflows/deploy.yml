name: Deploy Laravel to aaPanel

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest

    steps:
      - name: Install SSH Key
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} -p ${{ secrets.SSH_PORT }} << 'ENDSSH'

          PROJECT_DIR="/www/wwwroot/imobiliariaubatuba.com"
          REPO="git@github.com:${{ github.repository }}.git"

          cd \$PROJECT_DIR

          echo "📁 Verificando se existe repositório Git..."
          if [ ! -d ".git" ]; then
              echo "📥 Repositório não encontrado. Clonando o projeto..."
              git clone \$REPO .
          else
              echo "⬇️ Repositório encontrado. Atualizando..."
              git pull origin main
          fi

          echo "📦 Instalando dependências PHP..."
          composer install --no-interaction --prefer-dist --optimize-autoloader

          echo "📦 Instalando dependências Node..."
          npm install
          npm run build

          echo "🔧 Ajustando permissões..."
          chmod -R 775 storage bootstrap/cache
          chown -R www:www storage bootstrap/cache

          echo "🧹 Limpando caches Laravel..."
          php artisan optimize:clear
          php artisan optimize

          echo "🚀 Deploy finalizado!"

          ENDSSH